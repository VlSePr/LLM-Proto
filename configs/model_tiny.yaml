# Tiny model config — 30M parameters
# Quick prototyping and validation in Colab

vocab_size: 32000
dim: 512
n_layers: 6
n_heads: 8
n_kv_heads: 4
max_seq_len: 2048
dropout: 0.0
norm_eps: 1.0e-05
rope_theta: 10000.0
tie_embeddings: true
